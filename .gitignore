__pycache__/
*.pyc
.pytest_cache/
venv/
output/
.env

# Colecciones del usuario (boletas subidas progresivamente + rendiciones generadas,
# ver src/receipt_collections.py) — datos locales, no versionados.
colecciones/

# Artefactos de PyInstaller (ver app.spec/build.bat) — generados, no versionados.
# app.spec sí se versiona (es el build reproducible).
build/
dist/

# Archivo de secretos local con la API key de Anthropic (texto plano, ver src/api_key.py).
secrets.yaml
git s